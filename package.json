{
  "name": "pdf-editor-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=example-app",
    "build": "npm run build --workspace=@jmt-code/pdf-creator && npm run build --workspace=@jmt-code/pdf-editor && npm run build --workspace=example-app",
    "deploy": "npm run deploy --workspace=example-app"
  }
}
